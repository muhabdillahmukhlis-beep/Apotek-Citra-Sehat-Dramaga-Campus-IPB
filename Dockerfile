FROM php:8.2-fpm`n`nRUN apt-get update && apt-get install -y nginx libpng-dev libjpeg-dev libfreetype6-dev libzip-dev libonig-dev libxml2-dev zip unzip git curl && docker-php-ext-configure gd --with-freetype --with-jpeg && docker-php-ext-install pdo_mysql mbstring exif pcntl bcmath gd zip`n`nRUN echo "server { listen 80; root /var/www/html/public; index index.php index.html; location / { try_files `$uri `$uri/ /index.php?`$query_string; } location ~ \.php$ { include fastcgi_params; fastcgi_pass 127.0.0.1:9000; fastcgi_index index.php; fastcgi_param SCRIPT_FILENAME `$document_root`$fastcgi_script_name; } }" > /etc/nginx/sites-available/default`n`nWORKDIR /var/www/html`nCOPY . .`n`nRUN curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer`nRUN composer install --no-dev --optimize-autoloader`n`nRUN chown -R www-data:www-data /var/www/html/storage /var/www/html/bootstrap/cache`n`nEXPOSE 80`n`nCMD service nginx start && php-fpm
